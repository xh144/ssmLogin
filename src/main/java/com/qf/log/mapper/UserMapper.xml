<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qf.log.mapper.UserMapper">
    <select id="queryUser">
        SELECT * FROM user where uname=#{uname} and upwd=#{upwd}
    </select>
</mapper>